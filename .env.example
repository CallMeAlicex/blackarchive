# Copy this to .env and fill in values before running locally
# Railway sets these automatically via its dashboard

# REQUIRED: Change this to a long random string in production
SESSION_SECRET=change-me-to-something-long-and-random

# Optional: Override port (Railway sets PORT automatically)
PORT=3000

# Optional: Override where SQLite database file is stored
DATA_DIR=./data
